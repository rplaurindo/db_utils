# encoding: utf-8

module DBUtils
  module RDoc
    # qual a diferença entre < Rails e ::Rails
    class RdocGenerator < ::Rails::Generators::Base

      # include Sentinel::Engine.helpers
      # extend Sentinel::Engine.helpers

      # desc "Genereates RDoc in folder rdoc/#{engine_name}."

      # load "#{engine_root}/Rakefile"

    end
  end
end